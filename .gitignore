# Generated by the native build - never checked in (expo prebuild recreates it).
/ios/
/android/
node_modules/
.expo/
# Metro bundle output (`npm run bundle` / the CI bundle job).
/dist/
*.ipa
*.log

# Temp payload the AltStore source job pipes the releases API into.
/releases.json
